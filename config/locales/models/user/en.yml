en:
  activerecord:
    models:
      user: "User"
    attributes:
      user:
        login: "Username"
        email: "Email"
        password: "Password"
        password_confirmation: "Password confirmation"
    validations:
      user:
        login:
          invalid_format: "should use only letters, numbers, and underscore. Between 3 and 15 characters."
